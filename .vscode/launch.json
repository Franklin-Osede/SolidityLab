{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug Foundry Tests",
            "program": "${workspaceFolder}/node_modules/.bin/forge",
            "args": ["test", "-vvv"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
